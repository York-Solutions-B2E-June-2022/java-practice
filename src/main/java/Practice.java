public class Practice {

    // just like a java var needs to have a type, a function also needs a return type
    String hello() {
        return "hello";
    }

    // JS version
    /*
        function greet(name) {
            return 'hello' + name;
        }
     */
    // function vars in java also need a type!
    String greet(String name) {
        return "hello" + name;
    }
}


